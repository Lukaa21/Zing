# Zing Frontend

This is a small React application using Vite for the Zing game.

Local dev:

```powershell
pnpm --filter @zing/frontend install
pnpm --filter @zing/frontend dev
```

It connects to backend via `VITE_BACKEND_URL`.
